#!/bin/sh
# Strip IDE footer before the commit is recorded (portable; Git Bash / MSYS).
msg_file="$1"
[ -n "$msg_file" ] && [ -f "$msg_file" ] || exit 0
tmp="${msg_file}.strip.$$"
grep -v '^Made-with:' "$msg_file" > "$tmp" && mv "$tmp" "$msg_file"
